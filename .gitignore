# Legacy Prometheus wrappers (superseded by gsc_prometheus.sh)
gsc_container_prometheus.sh
gsc_docker_prometheus.sh

# macOS
.DS_Store

# Release bundles produced by `make bundle`
*.tar.xz
*.xz
*.zipv
# Runtime log output
*.log

# Claude Code local settings
./settings.local.json

# Go build artifacts (partition_growth/build/ binaries are intentionally tracked)
partition_growth/*.test

# SQLite databases — private test data, never upload
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# hcpcs_db local binary built without platform suffix (go build main.go)
hcpcs_db/hcpcs_db

# Temporary directories created by scripts
tmp/
temp/

# Stale release archive directory
archive/

# Distribution/Staging
release/
processhealth/

# Gemini CLI
*-cli-linux-amd64.tar.gz
gemini-cli-*
release.sh
